import { FC } from 'react';
import { Props as IconProps } from '../Icon';
interface Props {
    className?: string;
    text: JSX.Element | string;
    backgroundColor: string;
    icon?: IconProps['name'];
}
declare const StatusLabel: FC<Props>;
export default StatusLabel;
