import { PureComponent } from 'react';
interface DefaultProps {
    size: string;
    thickness: string;
    color: string;
}
interface Props extends DefaultProps {
    className?: string;
}
declare class Spinner extends PureComponent<Props> {
    static defaultProps: DefaultProps;
    render(): JSX.Element;
}
export default Spinner;
