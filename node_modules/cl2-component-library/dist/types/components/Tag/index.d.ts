import { FC, FormEvent } from 'react';
import { Props as IconProps } from '../Icon';
import { BoxMarginProps, BoxWidthProps } from '../Box';
declare type Props = {
    isAutoTag: boolean;
    isSelected: boolean;
    className?: string;
    text: JSX.Element | string;
    icon?: IconProps['name'];
    onIconClick?: (arg: FormEvent) => void;
    onTagClick?: (arg: FormEvent) => void;
} & BoxMarginProps & BoxWidthProps;
declare const Tag: FC<Props>;
export default Tag;
