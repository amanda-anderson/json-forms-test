import { PureComponent } from 'react';
import { InputSize } from '../../utils/typings';
export interface Props {
    value: string;
    placeholder: string;
    className?: string;
    onBlur?: () => void;
    onChange: (arg: string) => void;
    size?: InputSize;
}
declare class LocationInput extends PureComponent<Props> {
    handleSelect: (adress: string) => void;
    render(): JSX.Element;
}
export default LocationInput;
