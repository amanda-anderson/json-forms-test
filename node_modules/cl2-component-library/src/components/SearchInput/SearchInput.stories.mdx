import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { text, number, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import SearchInput from './';

<Meta title="Components/SearchInput" component={SearchInput} />

# SearchInput

<Canvas>
  <Story name="default">
    <div style={{ maxWidth: '400px' }}>
      <SearchInput
        type="text"
        value={text('Value', '')}
        debounce={number('Debounce', 500)}
        placeholder={text('Placeholder', 'placeholder')}
        ariaLabel={text('ARIA Label', 'aria label')}
        i18nRemoveSearchTermMessage="Remove search term"
        i18nSearchTermMessage="Search term: "
        i18nSearchTermBlankMessage="Search term: blank"
        onChange={action('input changed')}
        onChangeDebounced={action('debounced input changed')}
        size={boolean('Small', false) ? 'small' : 'normal'}
      />
    </div>
  </Story>
</Canvas>

<Props of={SearchInput} />
