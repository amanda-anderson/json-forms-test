import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import { text } from "@storybook/addon-knobs";
import Badge from './';

<Meta title="Components/Badge" component={Badge} />

# Badge

<Canvas>
  <Story name="default">
    <Badge>
      {text('Label', 'A Badge with label')}
    </Badge>
  </Story>
</Canvas>

<Canvas>
  <Story name="inverted">
    <Badge className="inverse">
      {text('Label', 'A Badge with label')}
    </Badge>
  </Story>
</Canvas>


<Props of={Badge} />
